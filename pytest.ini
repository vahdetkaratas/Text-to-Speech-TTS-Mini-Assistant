[pytest]
markers =
    smoke: lightweight smoke tests
    tts: network or model dependent TTS tests
testpaths = tests
filterwarnings =
    ignore::UserWarning:matplotlib.*
    ignore::UserWarning:pydub.*
    ignore::DeprecationWarning:pydub.*
